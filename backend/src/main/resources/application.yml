spring:
    application:
        name: NinoEdu

    datasource:
        url: ${DB_URL:jdbc:postgresql://localhost:5432/NinoEdu}
        username: ${DB_USERNAME:ninoedu}
        password: ${DB_PASSWORD:$H|FAvj2ZOvb.yu}
        driver-class-name: org.postgresql.Driver

    jpa:
        hibernate:
            ddl-auto: validate
        show-sql: true
        properties:
            hibernate:
                dialect: org.hibernate.dialect.PostgreSQLDialect

    flyway:
        enabled: true
        baseline-on-migrate: true

server:
    port: 8080

api:
    security:
        token:
            secret: ${JWT_SECRET:my-secret-key}